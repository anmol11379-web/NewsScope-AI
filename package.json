{
  "name": "newsscope-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "NewsScope AI — Verify News, Fight Misinformation",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
